console.log("************* nested ternary operator **************");

const auth = false
const canPay2 = true

console.log(auth ? canPay2 ? "Its auth and can pay" : "Its auth but can¡t pay " : "Its not auth");
