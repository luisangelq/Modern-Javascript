console.log("*******************walk through the Array**********************");

const month = ["January", "February", "March", "April", "May", "June"];

console.log(month.length);

for(let i = 0; i < month.length; i++ ){
    console.log("with for loop " + month[i]);
}